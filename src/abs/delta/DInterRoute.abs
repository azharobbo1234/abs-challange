delta DInterRoute;
uses ABS.Framework.Route;

modifies class RouteConfigImpl {

    modifies String routeInter(String url) {

		String result = case url {
            "/account/transfer-inter.abs" => "MAccountResource.AccountResourceImpl@transferInter";
            _ => "";
        };

        return result;
    }
}