delta DTransferIntraBank;
uses MAccountModel;

modifies class AccountImpl {
    adds transferIntra(AccountImpl other, Int amount, Int fee) {
        withdraw(amount+fee)
        other.deposit(amount)
    }
}