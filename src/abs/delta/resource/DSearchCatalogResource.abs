delta DSearchCatalogResource;
uses MCatalogItemResource;


modifies interface CatalogItemResource{
    adds List<CatalogItem> searchCatalog(ABSHttpRequest request);
}

modifies class CatalogItemResourceImpl {
    
    adds List<CatalogItem> searchCatalog(ABSHttpRequest request){

        CatalogItemService service = new local CatalogItemServiceImpl();
        return service.listSearch(request);

    }
	
}