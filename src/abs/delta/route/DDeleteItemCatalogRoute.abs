delta DDeleteItemCatalogRoute;
uses ABS.Framework.Route;

modifies class RouteConfigImpl {

    modifies String routeDeleteItemCatalog(String url) {

		String result = case url {
        
            "/api/catalog/delete-item.abs" => "MCatalogItemResource.CatalogItemResourceImpl@delete";
        };

        return result;
    }
}