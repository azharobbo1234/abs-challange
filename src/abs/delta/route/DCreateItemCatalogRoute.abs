delta DCreateItemCatalogRoute;
uses ABS.Framework.Route;

modifies class RouteConfigImpl {

    modifies String routeCreateItemCatalog(String url) {

		String result = case url {
        
            "/api/catalog/create-item.abs" => "MCatalogItemResource.CatalogItemResourceImpl@create";
        };

        return result;
    }
}