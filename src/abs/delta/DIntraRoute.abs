delta DIntraRoute;
uses ABS.Framework.Route;

modifies class RouteConfigImpl {

    modifies String routeIntra(String url) {

		String result = case url {
            "/account/transfer-intra.abs" => "MAccountResource.AccountResourceImpl@transferIntra";
            _ => "";
        };

        return result;
    }
}