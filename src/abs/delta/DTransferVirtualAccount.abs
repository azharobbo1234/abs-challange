delta DTransferVirtualAccount;
uses MAccountModel;

modifies class AccountImpl {
    adds transferVirtualAccount(AccountImpl virtual, Int amount) {
        withdraw(amount)
        virtual.deposit(amount)
    }
}